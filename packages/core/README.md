# @dash/core

Shared TypeScript types and helpers for Dash:

- `DataAdapter` and `AdapterQuery` — warehouse contract
- `ChartSpec` — JSON charts for the UI and MCP
- Dashboard / widget documents
- API token format helpers (`dash_live_…`)

Adapters and `@dash/web` depend on this package. There is no runtime warehouse code here.
